import {StyleSheet} from 'react-native';

const Theme = StyleSheet.create({
    h1: {
        fontSize: 38,
    },

    body2: {
        fontSize: 16,
        color: 'white',
    },
});

export {Theme};
